---
 - name: Host configuration
   hosts: localhost

   vars:
   - ssh_user: "root"
   - ssh_pass: "P@ssw0rd123"

   tasks:
   - name: Register a new value
     command : date

   - add_host:
       hostname: orc-db-host
       ansible_ssh_host: "10.188.191.92"
       ansible_ssh_port: 22


 - name: Host configuration
   hosts: orc-db-host

   tasks:
   - name: Register a new value
     command : date

